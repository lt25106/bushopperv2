{
  "name": "bushopper",
  "version": "2.0.0",
  "description": "",
  "scripts": {
    "watch": "esbuild *.s.ts --bundle --minify --format=esm --target=esnext --splitting --outdir=docs --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@maptiler/sdk": "^3.8.0",
    "firebase": "^12.4.0"
  }
}
